<%= link_to 'Back', parrent_page_path(@page) %> |
<%= link_to 'Edit', edit_page_path(@page) %> |
<%= link_to 'Del', del_page_path(@page), method: :delete, data: { confirm: 'Are you sure?' } %> |
<%= link_to 'Add', new_page_path(@page) %>

<%= render 'tree', :children => @children %>

<h1>
	<%= @page.title %>
</h1>

<div class="content">
	<%= @page.text.html_safe %>
</div>
